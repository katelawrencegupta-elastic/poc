# Kibana dashboard definitions

JSON payloads for the Kibana Dashboards API (`PUT /api/dashboards/{id}`).

| File | Dashboard ID |
|------|----------------|
| `geh-fleet-overview.json` | `geh-fleet-overview` |
| `geh-hospital-detail.json` | `geh-hospital-detail` |

Deploy:

```bash
python -m geh_synthetic dashboards
```
